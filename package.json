{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --nohoist \"**\"",
    "clean": "lerna clean",
    "build": "lerna run build"
  }
}
